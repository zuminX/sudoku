<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sudoku.mapper.UserMapper">
  <resultMap id="BaseResultMap" type="com.sudoku.model.entity.User">
    <!--@Table `user`-->
    <id column="id" jdbcType="INTEGER" property="id"/>
    <result column="username" jdbcType="VARCHAR" property="username"/>
    <result column="password" jdbcType="VARCHAR" property="password"/>
    <result column="nickname" jdbcType="VARCHAR" property="nickname"/>
    <result column="enabled" jdbcType="BOOLEAN" property="enabled"/>
  </resultMap>

  <resultMap extends="BaseResultMap" id="UserWithRole" type="com.sudoku.model.entity.User">
    <collection ofType="com.sudoku.model.entity.Role" property="roles">
      <id column="rid" property="id"/>
      <result column="rname" property="name"/>
      <result column="rnameZh" property="nameZh"/>
    </collection>
  </resultMap>

  <sql id="Base_Column_List">
    id,
    username,
    `password`,
    nickname,
    enabled
  </sql>

  <select id="selectByUsername" resultMap="BaseResultMap">
    select *
    from `user`
    where username = #{username,jdbcType=VARCHAR}
  </select>

  <select id="selectWithRoleByUsername" resultMap="UserWithRole">
    select user.*, r.`id` as rid, r.`name` as rname, r.name_zh as rnameZh
    from user
           inner join user_role ur on ur.user_id = user.id
           inner join role r on r.`id` = ur.role_id
    where username = #{username,jdbcType=VARCHAR}
  </select>
</mapper>